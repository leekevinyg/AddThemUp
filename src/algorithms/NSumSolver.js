"use strict";

/* the following function determines if any non zero n-element subset of
the array sums to the target sum */
var NSumSolver = function(arrayOfPossibleNumbers, targetSum) {

  // stub
  return false;

};

module.exports = NSumSolver;
